.page-header
  = link_to product_types_path, :class => 'btn btn-default' do
    %span.glyphicon.glyphicon-list-alt
    Back
  = link_to edit_product_type_path(@product_type), :class => 'btn btn-primary' do
    %span.glyphicon.glyphicon-pencil
    Edit
  %h1 Show product_type

%dl.dl-horizontal
  %dt Name:
  %dd= @product_type.name
